{
  "id": "spread-rest",
  "courseId": "javascript-es6",
  "title": "Spread and Rest",
  "summary": "The spread operator (...) expands an array or object into its individual elements. The rest parameter (...) collects multiple values into a single array. Both use the same ... syntax but in opposite directions: spread expands, rest collects. In React, spread is essential for immutable state updates — you always return a new array or object rather than mutating the existing one.",
  "keyPoints": [
    "Spread arrays: [...arr1, ...arr2] — creates a new combined array",
    "Add to end: [...watchlist, newMovie]  |  Add to front: [newMovie, ...watchlist]",
    "Spread objects: { ...obj, newProp: value } — creates a new object with merged properties",
    "Later keys overwrite earlier ones: { ...movie, year: 2011 } overrides year",
    "Rest in parameters: function fn(first, ...rest) — rest collects remaining args into an array",
    "Rest in destructuring: const [head, ...tail] = arr",
    "Spread is SHALLOW — nested objects are still shared by reference",
    "In React: setWatchlist(prev => [...prev, newMovie])  — never mutate state directly"
  ],
  "externalLinks": [
    {
      "label": "MDN — Spread syntax",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Spread_syntax"
    },
    {
      "label": "MDN — Rest parameters",
      "url": "https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions/rest_parameters"
    }
  ],
  "exercises": [
    {
      "id": "sr-ex-01",
      "type": "multiple-choice",
      "difficulty": "beginner",
      "prompt": "Given:\nconst watchlist = ['Inception', 'Dune']\n\nWhich expression creates a NEW array with 'Tenet' added at the end, without mutating watchlist?",
      "options": [
        "watchlist.push('Tenet')",
        "[...watchlist, 'Tenet']",
        "watchlist + ['Tenet']",
        "watchlist.concat = ['Tenet']"
      ],
      "answer": "[...watchlist, 'Tenet']",
      "explanation": "Spread creates a new array containing all elements of watchlist followed by 'Tenet'. push mutates the original array, which is never acceptable for React state."
    },
    {
      "id": "sr-ex-02",
      "type": "multiple-choice",
      "difficulty": "beginner",
      "prompt": "Given:\nconst movie = { title: 'Inception', year: 2010 }\nconst updated = { ...movie, year: 2011 }\n\nWhat is updated.year?",
      "options": ["2010", "2011", "undefined", "Both 2010 and 2011"],
      "answer": "2011",
      "explanation": "When spreading an object, later properties overwrite earlier ones with the same key. year: 2011 comes after ...movie, so it wins."
    },
    {
      "id": "sr-ex-03",
      "type": "multiple-choice",
      "difficulty": "beginner",
      "prompt": "What does the rest parameter do in this function?\n\nfunction logMovies(first, ...rest) {}",
      "options": [
        "rest holds the first argument",
        "rest collects all arguments after the first into an array",
        "rest makes the remaining parameters optional",
        "rest spreads the arguments into individual variables"
      ],
      "answer": "rest collects all arguments after the first into an array",
      "explanation": "The rest parameter (...rest) gathers everything after the named parameters into a single array. logMovies('Inception', 'Dune', 'Tenet') gives first = 'Inception' and rest = ['Dune', 'Tenet']."
    },
    {
      "id": "sr-ex-04",
      "type": "multiple-choice",
      "difficulty": "intermediate",
      "prompt": "Given:\nconst watchlist = [\n  { id: 1, title: 'Inception' },\n  { id: 2, title: 'Dune' },\n  { id: 3, title: 'Tenet' }\n]\n\nWhich expression creates a new array without the movie with id 2, without mutating the original?",
      "options": [
        "watchlist.splice(1, 1)",
        "watchlist.filter(m => m.id !== 2)",
        "delete watchlist[1]",
        "watchlist.length = watchlist.length - 1"
      ],
      "answer": "watchlist.filter(m => m.id !== 2)",
      "explanation": "filter returns a new array containing only elements where the callback returns true. splice and delete mutate the original array. Reducing length also mutates and doesn't target a specific item by id."
    },
    {
      "id": "sr-ex-05",
      "type": "multiple-choice",
      "difficulty": "intermediate",
      "prompt": "Given:\nconst movie = { title: 'Dune', isWatched: false, rating: 8.0 }\n\nWhich creates a new object with isWatched set to true, without mutating the original?",
      "options": [
        "movie.isWatched = true",
        "Object.assign(movie, { isWatched: true })",
        "const updated = { ...movie, isWatched: true }",
        "movie['isWatched'] = true"
      ],
      "answer": "const updated = { ...movie, isWatched: true }",
      "explanation": "Spread copies all existing properties into a new object, then isWatched: true overrides the old value. Options A, B, and D all mutate the original movie object directly."
    }
  ]
}
