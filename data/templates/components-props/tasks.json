[
  {
    "id": "task1",
    "description": "Pass props to ONE ArticleComp (image must use the flowerUrl variable)",
    "points": 25,
    "check": {
      "type": "code-contains",
      "pattern": "<ArticleComp[^>]*image\\s*=\\s*\\{\\s*flowerUrl\\s*\\}"
    }
  },
  {
    "id": "task2",
    "description": "ADD the other articles to the DOM. Map over articlesList to render an ArticleComp for each item",
    "points": 25,
    "check": {
      "type": "code-contains",
      "pattern": "articlesList\\.map\\s*\\("
    }
  },
  {
    "id": "task3",
    "description": "Add a unique key prop to each rendered ArticleComp from the articlesList so React works correctly",
    "points": 25,
    "check": {
      "type": "code-contains",
      "pattern": "\\bkey\\s*=\\s*[{\"']"
    }
  },
  {
    "id": "task4",
    "description": "Define the ArticleProps type with title, description, and image fields",
    "points": 25,
    "check": {
      "type": "code-contains",
      "pattern": "type\\s+ArticleProps\\s*=\\s*\\{(?=[\\s\\S]*title\\s*:\\s*string)(?=[\\s\\S]*description\\s*:\\s*string)(?=[\\s\\S]*image\\s*:\\s*string)"
    }
  }
]
